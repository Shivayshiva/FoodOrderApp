import React from "react";
import "./Total.css";

export default function Total(props) {
  // const totalorder = (props.foodarray,props.fooddefault) => {
  //     props.foodarray.filter((val, props.fooddefault) => {return val.food === props.food })
  // }
  // const totalorder=props.foodarray.filter((x, props.fooddefault)=>{return x.food===props.fooddefault})
  return <div className="font">Total: 5</div>;
}
